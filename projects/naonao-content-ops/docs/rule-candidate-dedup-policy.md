# Rule Candidate Dedup Policy

## 目标
避免规则候选爆炸，优先复用与升级既有规则。

## 去重判定原则
1. 标题相似 ≠ 重复。
2. 仅当「指令对象 + 条件 + scope」一致时，判定为重复候选。
3. 若新候选只是旧规则改写表达，优先走 MODIFY/MERGE，不走 ADD。

## 升级触发
- 连续 3 次同类纠错事件触发时，优先升级现有规则。
- 同类事件证据不足时，保持 candidate 或 TEST_MORE，不直接 active。

## 动作优先级
1. MERGE
2. MODIFY
3. DEPRECATE（旧规则）
4. ADD（最后手段）

## 与预算联动
- 每次新增规则建议同时给出可替换旧规则（1进1出优先）。
- 若无可替换对象，必须附“为何不能复用”的说明。

## 首次试运行（Patch Pack E）
基于第一轮回归候选，按以下动作分类：
- 新增（ADD）：仅在无可复用旧规则且证据充分时
- 合并（MERGE）：候选与现有规则对象/条件/scope一致但表达不同
- 修改已有（MODIFY）：同规则主旨，需补条件或边界
- 拒绝（REJECT）：重复、过于抽象、证据不足

本轮状态：
- 真实链路运行已完成（3 组 run done）
- 因 payload 可见性受限，候选规则明细暂未自动抓取
- 下一步：启用可见性或回调落盘后执行 ADD/MERGE/MODIFY/REJECT 明细判定
