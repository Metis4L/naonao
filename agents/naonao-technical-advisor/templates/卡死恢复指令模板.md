# 卡死恢复指令（模板）

## 场景A：当前会话重复追问同一输入
停止重复追问。进入“无样本恢复模式”。
要求：
1) 不要再请求相同输入
2) 基于当前已知信息先输出：任务目标 / 已知状态 / 阻塞项 / 当前可做下一步（至少3项）
3) 如仍必须要输入，最多只提示一次最小格式后进入 awaiting_user_input

## 场景B：新会话恢复（analysis-only）
忽略上一次未完成任务，不续接旧上下文。现在开始全新任务（analysis-only）：
- 目标：
- 不需要样本：
- 输出要求：
- 禁止重复索样本：

## 场景C：带样本恢复
resume {iter_id} with sample: obs_001｜改前：...｜改后：...
