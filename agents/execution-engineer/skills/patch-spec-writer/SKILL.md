---
name: patch-spec-writer
description: 输出给 OpenClaw 可执行的逐文件补丁说明（路径、改动类型、具体内容、验收标准），用于新建或修改 Agent/Skill/模板/规则。
user-invocable: true
disable-model-invocation: false
---

# 补丁执行单生成器（Patch Spec Writer）

你的任务是给出“OpenClaw 能执行”的逐文件修改说明，而不是泛泛建议。

## 适用场景

- 创建完整 agent 包
- 修改已有 agent / skills / templates / memory 文件
- 增加功能（新skill、新模板、新规则）
- 局部修复（冲突、跑偏、结构问题）

---

## 必须遵守的输出要求

1. 按路径逐文件写
2. 每个文件写明“改什么/为什么”
3. 指明操作类型：
- 新建
- 追加
- 替换某段
- 插入某块
4. 给出可粘贴内容（如果需要）
5. 给出验收标准（至少1条）

禁止只给抽象方向不落文件。

---

## 路径作用域与执行闸门（必须）

- 输出执行单时必须标注 `project scope`。
- 先校验目标路径是否位于允许范围（agents/projects/workspaces）。
- 未获用户明确确认前，只输出执行单，不直接修改文件。
- 涉及替换整段、删除、迁移时，必须要求二次确认。

## 信息不足时（必须）

- 先输出可执行的最小补丁草案（含假设）。
- 再提最多 5 个高价值问题补齐关键缺口。

## 固定输出格式（每次按此格式）

# 执行单（给 OpenClaw）

## 0. 执行目标（先给）
- 本次目标：
- 影响范围：
- 是否新建文件：
- 是否涉及破坏性修改（有/无）：

## 1. 文件修改清单（按路径）
### [路径1]
- 操作类型：
- 目的：
- 修改说明：
- 具体内容（可粘贴）：
- 验收标准：

### [路径2]
- 操作类型：
- 目的：
- 修改说明：
- 具体内容（可粘贴）：
- 验收标准：

## 2. 执行顺序（建议）
1. ...
2. ...
3. ...

## 3. 风险与回滚（如适用）
- 风险：
- 回滚方法：

## 4. 执行后回报格式（要求 OpenClaw 返回）
- 已修改文件列表
- 每个文件变更摘要
- 是否有冲突/失败
- 下一步建议
- changelog 记录内容（写入 `agents/execution-engineer/memory/changelog.md`）
- 若涉及策略取舍，decisions 记录内容（写入 `agents/execution-engineer/memory/decisions.md`）
