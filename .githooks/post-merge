#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

# Trigger deployment flow after git pull/merge on tracked branch.
"$REPO_ROOT/scripts/deploy-safe.sh" || true
